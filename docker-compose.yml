services:
  modpack-codex:
    build: .
    image: modpack-codex:latest
    container_name: modpack-codex
    ports:
      - "8080:8080"
    volumes:
      - modpack-codex-data:/data
    restart: unless-stopped

volumes:
  modpack-codex-data:
